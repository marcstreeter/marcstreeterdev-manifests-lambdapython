# Changes here will be overwritten by Copier
# This file records the answers provided when generating this project
# It's used by Copier to track what values were used and enable future updates

# Project configuration
project_name: "{{ project_name }}"
project_slug: "{{ project_slug }}"
project_kebab: "{{ project_kebab }}"
project_chart: "{{ project_chart }}"
project_type: "{{ project_type }}"
project_description: "{{ project_description }}"

# Author information
author_name: "{{ author_name }}"
author_email: "{{ author_email }}"
github_username: "{{ github_username }}"
github_repo: "{{ github_repo }}"

# Lambda configuration
lambda_timeout: {{ lambda_timeout }}
lambda_memory_size: {{ lambda_memory_size }}
cloudwatch_log_retention: {{ cloudwatch_log_retention }}
aws_region: "{{ aws_region }}"

# Python configuration
python_version: "{{ python_version }}"

# Interface configuration
interface_port: {{ interface_port }}
interface_port_name: "{{ interface_port_name }}"
tilt_interface_port: {{ tilt_interface_port }}

# License
open_source_license: "{{ open_source_license }}"

# Copier metadata
_copier_hash: "{{ _copier_hash }}"
_copier_version: "{{ _copier_version }}"
